apiVersion: v1
kind: Service
metadata:
  name: go-web-app
  labels:
    app: go-web-app
spec:
  type: ClusterIP
  selector:
    app: go-web-app
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
